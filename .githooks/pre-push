#!/usr/bin/env bash
# Block a push if any suite is red.
#
# Exists because a red commit actually landed once (368c8f6) — the three
# suites were only ever run by hand. Bypass in an emergency: git push --no-verify
set -e
cd "$(git rev-parse --show-toplevel)"
echo "pre-push: running make check…"
make check
